version: "3"

services:
  haskell:
    image: haskell:8.6.3
    volumes:
      - ./exercises:/exercises
    working_dir: /exercises
  submit:
    build:
      context: .
      dockerfile: submit.dockerfile
    env_file:
      - mat-numbers.env
    volumes:
      - ./:/repo
    working_dir: /repo
